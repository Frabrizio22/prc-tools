name = "prc-checkout"
main = "cloudflare-worker-FIXED.js"
compatibility_date = "2024-01-01"

# Environment variables (secrets - don't put actual values here)
# Set these via: wrangler secret put VARIABLE_NAME
[env.production]
vars = { }

# These should already be set as secrets:
# - BANKFUL_USERNAME
# - BANKFUL_PASSWORD
# - GOOGLE_SHEET_WEBHOOK_URL
# - TELEGRAM_BOT_TOKEN
# - TELEGRAM_CHAT_ID
